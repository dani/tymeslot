# Tymeslot Environment Configuration
# Copy this file to .env and fill in your values

# ====================
# REQUIRED SETTINGS
# ====================

# REQUIRED: Must be at least 64 characters. Generate with: openssl rand -base64 64 | tr -d '\n'
SECRET_KEY_BASE=

# REQUIRED: Your domain name (e.g., tymeslot.yourdomain.com or localhost for local testing)
PHX_HOST=

# REQUIRED: Database configuration (for Docker/self-hosted deployments)
POSTGRES_DB=tymeslot
POSTGRES_USER=tymeslot
POSTGRES_PASSWORD=

# ====================
# APPLICATION SETTINGS
# ====================

# Port (defaults to 4000)
PORT=4000

# Listen IP address (defaults to :: which accepts all IPv4 and IPv6 connections)
# Common values:
#   :: (default) - All interfaces (IPv4 and IPv6)
#   0.0.0.0 - All IPv4 interfaces only
#   127.0.0.1 - Localhost only (useful for service mesh deployments)
#   ::1 - IPv6 localhost only
# LISTEN_IP=::

# Deployment type (docker/cloudron)
DEPLOYMENT_TYPE=docker

# Database pool size (defaults to 10 for Docker)
# DATABASE_POOL_SIZE=10

# ====================
# EMAIL CONFIGURATION
# ====================

# Email adapter: "test" (testing/docker - silently accepts), "postmark" (recommended for production), or "smtp"
EMAIL_ADAPTER=test

# Common Email Settings
EMAIL_FROM_NAME="Tymeslot"
EMAIL_FROM_ADDRESS="hello@yourdomain.com"
# Optional: Defaults to EMAIL_FROM_ADDRESS if not set
# EMAIL_SUPPORT_ADDRESS="support@yourdomain.com"
# EMAIL_CONTACT_RECIPIENT="info@yourdomain.com"

# Postmark Configuration (when EMAIL_ADAPTER=postmark)
# Sign up at https://postmarkapp.com/
# POSTMARK_API_KEY=your_postmark_api_key

# SMTP Configuration (when EMAIL_ADAPTER=smtp)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your_email@gmail.com
# SMTP_PASSWORD=your_app_password

# ====================
# OAUTH INTEGRATIONS
# ====================

# IMPORTANT: These OAuth credentials are REQUIRED for calendar and video integrations
# Users must set up their own OAuth apps
# For detailed setup instructions, see README-Docker.md (OAuth Provider Setup section)

# Google OAuth (for Google Calendar + Google Meet)
# Setup: https://console.cloud.google.com/
# 1. Create project and enable Google Calendar API
# 2. Create OAuth 2.0 credentials (Web application)
# 3. Add redirect URI: https://yourdomain.com/auth/google/callback
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_STATE_SECRET=  # Self-generated random string (openssl rand -base64 32)

# GitHub OAuth (for GitHub authentication)
# Setup: https://github.com/settings/developers
# 1. Create new OAuth App
# 2. Set Authorization callback URL: https://yourdomain.com/auth/github/callback
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=

# Microsoft OAuth (for Outlook Calendar + Microsoft Teams)
# IMPORTANT: One OAuth app is used for BOTH Outlook Calendar AND Teams
# Setup: https://portal.azure.com/ (Microsoft Entra ID → App registrations)
# 1. Register new application (Accounts in any organizational directory and personal Microsoft accounts)
# 2. Add BOTH redirect URIs under Authentication → Web platform:
#    - https://yourdomain.com/auth/outlook/calendar/callback
#    - https://yourdomain.com/auth/teams/video/callback
# 3. Add API permissions (Microsoft Graph → Delegated):
#    - Calendars.ReadWrite, User.Read, offline_access, openid, profile
# 4. Create Client Secret under Certificates & secrets
# For detailed instructions, see README-Docker.md (Microsoft OAuth Setup section)
# OUTLOOK_CLIENT_ID=
# OUTLOOK_CLIENT_SECRET=
# OUTLOOK_STATE_SECRET=  # Self-generated random string (openssl rand -base64 32)

# ====================
# SOCIAL AUTHENTICATION
# ====================

# Enable social login/signup (defaults to false)
# If enabled, you MUST configure the corresponding OAuth credentials above
# Note: Setting these to false still allows OAuth integrations to work for calendar/video
# Users can still connect Google Calendar even if ENABLE_GOOGLE_AUTH=false
ENABLE_GOOGLE_AUTH=false
ENABLE_GITHUB_AUTH=false